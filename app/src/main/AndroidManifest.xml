<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sriram.marshallgoldsmithapp">

    <application
        android:allowBackup="true"
        android:icon="@drawable/goldsmith"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity android:name=".Question1Activity" />
        <activity android:name=".Question2Activity" />
        <activity android:name=".Question3Activity"></activity>
    </application>

</manifest>